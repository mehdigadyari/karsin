import { makeStyles } from '@material-ui/core'

export default makeStyles(() => ({
  root: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  },
  HeadIcon: {
    marginTop: 130,
    width: '60%',
    height: 600
  },
  karTitle: {
    fontSize: 20,
    position: 'absolute',
    top: 70
  }
}))
